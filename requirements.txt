# FinancialAR OSINT - Requirements
# Python 3.8+

# Web Framework
Flask==3.1.2
Werkzeug==3.1.4
Jinja2==3.1.6

# HTTP Requests
requests==2.32.5
truststore>=0.8.0  # Certificados del sistema (Windows/macOS/Linux) - RECOMENDADO
certifi>=2024.0.0  # Certificados SSL actualizados (fallback si truststore falla)
urllib3>=2.0.0     # Cliente HTTP con soporte SSL mejorado

# Nota: SQLite3 viene incluido con Python (no requiere instalación)
# Nota: difflib viene incluido con Python (no requiere instalación)
